version: 2

models:
  - name: staging_emp
    description: "Staging employee model"

    columns:
      - name: emp_id
        description: "Unique employee identifier"

      - name: emp_name
        description: "Employee full name"

      - name: dept_id
        description: "Department identifier"